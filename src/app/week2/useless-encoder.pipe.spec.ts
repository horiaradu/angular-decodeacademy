import { UselessEncoderPipe } from './useless-encoder.pipe';

describe('UselessEncoderPipe', () => {
  it('create an instance', () => {
    const pipe = new UselessEncoderPipe();
    expect(pipe).toBeTruthy();
  });
});
